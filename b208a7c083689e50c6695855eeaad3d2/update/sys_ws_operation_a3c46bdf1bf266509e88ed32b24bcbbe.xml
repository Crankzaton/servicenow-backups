<?xml version="1.0" encoding="UTF-8"?><record_update table="sys_ws_operation">
    <sys_ws_operation action="INSERT_OR_UPDATE">
        <active>true</active>
        <consumes>application/json,application/xml,text/xml</consumes>
        <consumes_customized>false</consumes_customized>
        <default_operation_uri/>
        <enforce_acl>cf9d01d3e73003009d6247e603f6a990</enforce_acl>
        <http_method>GET</http_method>
        <name>Aggregate</name>
        <operation_script><![CDATA[// API for the reconciliation of SN with EL for Grafana dashboard

(function process( /*RESTAPIRequest*/ request, /*RESTAPIResponse*/ response) {

    var queryParams = request.queryParams;

    //Validating query parameters
    if (!queryParams) {
        response.setError(new sn_ws_err.BadRequestError('Missing Paramaters'));
        return;
    }

    if (!queryParams.type) {
        response.setError(new sn_ws_err.BadRequestError('Missing Paramater [type]. Expected value [period] or [duration]'));
        return;
    }
    if (queryParams.type[0] !== 'duration' && queryParams.type[0] !== 'period') {
        response.setError(new sn_ws_err.BadRequestError('Incorrect Paramater value for [type]. Expected value [period] or [duration]'));
        return;
    }
    if (queryParams.type[0] == 'period' && (!queryParams.start || !queryParams.end)) {
        response.setError(new sn_ws_err.BadRequestError('For type [period], missing paramater [start] or [end]'));
        return;
    }
    if (queryParams.type[0] == 'duration' && (!queryParams.end || !queryParams.dur)) {
        response.setError(new sn_ws_err.BadRequestError('For type [duration], missing paramater [end] or [dur]'));
        return;
    }

    if (queryParams.type[0] == 'duration') {

        var end = queryParams.end[0];
        var dur = queryParams.dur[0];


        var start = new GlideDateTime(end);
        start.addSeconds(Math.abs(parseInt(dur) * 60) * -1); //Calculating start from duration in minutes 

        var ga = new GlideAggregate('x_hsb11_hsbc_mss_o_mss_event_inbound');
        ga.addEncodedQuery("sys_created_on>=" + start + "^sys_created_on<" + end);
        ga.addAggregate('COUNT');
        ga.query();

        if (ga.next()) {
            response.setBody({
                type: queryParams.type[0],
                start: start.getValue(),
                end: end,
                count: ga.getAggregate('COUNT')
            });
        } else {
            return new sn_ws_err.NotFoundError('The resource you requested is not part of the API.');
        }
    }

    if (queryParams.type[0] == 'period') {

        //Validate date format: yyyy-MM-dd HH:mm:ss
        var dateReg = /^\d{4}-\d{2}-\d{2}\s\d{2}:\d{2}:\d{2}$/;
        if (queryParams.start[0].match(dateReg) === null) {
            new sn_ws_err.BadRequestError('Invalid Paramater [start]. Must be in format yyy-MM-dd HH:mm:ss');
            return;
        }
        if (queryParams.end[0].match(dateReg) === null) {
            new sn_ws_err.BadRequestError('Invalid Paramater [end]. Must be in format yyy-MM-dd HH:mm:ss');
            return;
        }

        var start_time = queryParams.start[0];
        var end_time = queryParams.end[0];

        var ga = new GlideAggregate('x_hsb11_hsbc_mss_o_mss_event_inbound');
        ga.addEncodedQuery("sys_created_on>=" + start_time + "^sys_created_on<" + end_time);
        ga.addAggregate('COUNT');
        ga.query();

        if (ga.next()) {
            response.setBody({
                type: queryParams.type[0],
                start: start_time,
                end: end_time,
                count: ga.getAggregate('COUNT')
            });

        } else {
            return new sn_ws_err.NotFoundError('The resource you requested is not part of the API.');
        }
    }

})(request, response);]]></operation_script>
        <operation_uri>/api/hsb11/ce_inbound_events/aggregate</operation_uri>
        <produces>application/json,application/xml,text/xml</produces>
        <produces_customized>false</produces_customized>
        <relative_path>/aggregate</relative_path>
        <request_example/>
        <requires_acl_authorization>true</requires_acl_authorization>
        <requires_authentication>true</requires_authentication>
        <requires_snc_internal_role>true</requires_snc_internal_role>
        <short_description/>
        <sys_class_name>sys_ws_operation</sys_class_name>
        <sys_created_by>45423094@hsbc.com</sys_created_by>
        <sys_created_on>2025-07-23 08:48:36</sys_created_on>
        <sys_id>a3c46bdf1bf266509e88ed32b24bcbbe</sys_id>
        <sys_mod_count>6</sys_mod_count>
        <sys_name>Aggregate</sys_name>
        <sys_package display_value="Now Utils" source="x_938076_now_utils">b208a7c083689e50c6695855eeaad3d2</sys_package>
        <sys_policy/>
        <sys_scope display_value="Now Utils">b208a7c083689e50c6695855eeaad3d2</sys_scope>
        <sys_update_name>sys_ws_operation_a3c46bdf1bf266509e88ed32b24bcbbe</sys_update_name>
        <sys_updated_by>45423094@hsbc.com</sys_updated_by>
        <sys_updated_on>2025-07-30 10:11:57</sys_updated_on>
        <web_service_definition display_value="CE Inbound Events">5004639f1bf266509e88ed32b24bcb6f</web_service_definition>
        <web_service_version/>
    </sys_ws_operation>
</record_update>
